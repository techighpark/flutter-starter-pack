import 'package:flutter/material.dart';

class ButtonTypeView extends StatelessWidget {
  const ButtonTypeView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
